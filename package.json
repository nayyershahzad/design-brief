{
  "name": "design-directions",
  "version": "0.2.0",
  "type": "module",
  "description": "Explore UI design directions locally, lock one, and export shadcn-native tokens + a DESIGN_SPEC.md contract.",
  "license": "MIT",
  "author": "Nayyer Shahzad",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "design-brief": "packages/cli/dist/bin.js"
  },
  "files": [
    "packages/cli/dist",
    "packages/playground/dist"
  ],
  "scripts": {
    "build": "npm run build -w @design-brief/core && node scripts/build-remix.mjs && npm run build -w @design-brief/cli && npm run build -w @design-brief/playground",
    "prepare": "npm run build",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspace @design-brief/playground --if-present"
  }
}
