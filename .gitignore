node_modules
dist
.DS_Store
.env
.env.local
*.log
coverage
.design-brief/

# Stray credential file accidentally copied into the brief folder — never commit.
user.md

# Internal build/agent spec — kept local, not published to the public repo.
CLAUDE.md
